{
  "name": "labelle-web",
  "version": "1.0.0",
  "private": true,
  "description": "Web interface for labelle DYMO label printers",
  "scripts": {
    "dev": "concurrently -n client,server -c blue,green \"npm run dev -w client\" \"python3 server/app.py\"",
    "build": "npm run build -w client",
    "start": "python3 server/app.py",
    "install:all": "npm install && pip install -r server/requirements.txt"
  },
  "workspaces": [
    "client"
  ],
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
