flask
flask-cors
python-dotenv
waitress

# labelle's runtime deps (excluding PyQt6 which is only needed for the desktop GUI)
platformdirs
Pillow>=8.1.2,<11
PyQRCode>=1.2.1,<2
python-barcode>=0.13.1
pyusb
darkdetect
typer>=0.16.0

# dev / test
pytest
