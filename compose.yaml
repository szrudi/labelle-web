services:
  labelle-web:
    image: ghcr.io/szrudi/labelle-web:latest
    build: .
    ports:
      - "${PORT:-5000}:${PORT:-5000}"
    env_file:
      - path: .env
        required: false
    environment:
      - PYTHONUNBUFFERED=1
    # USB passthrough for DYMO printer access
    volumes:
      - /dev/bus/usb:/dev/bus/usb
      # Mount output directory for virtual printers
      - ./output:/app/output
    # Required for USB device access from the container
    privileged: true
    restart: unless-stopped
